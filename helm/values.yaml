namespace:
  name: demo
 
image:
  repository: nginx
  tag: alpine
  pullPolicy: IfNotPresent

resources:
   requests:
      cpu:50m
      mmemory:256Mi

replicaCount: 3

service:
   type:
     ClusterIP
   port: 8111

configMap:
   data:
     versionn: alpine
